/** Buffer names loaded from `sound_manifest.json` → `damage` (see `loadSoundManifest.ts`). */

export function getDamageFallBigSound(): string {
  return "dmg_fall_big";
}

export function getDamageFallSmallSound(): string {
  return "dmg_fall_small";
}

export function getDamageHitSound(): string {
  return "dmg_hit";
}
